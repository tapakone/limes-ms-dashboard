name: Fetch XAUUSD Data (legacy)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "This legacy workflow is intentionally disabled (manual only)."
          echo "Use Fetch Assets Data instead."
